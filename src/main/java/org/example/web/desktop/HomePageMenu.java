package org.example.web.desktop;


import org.example.web.common.AbstractMenu;
import org.openqa.selenium.SearchContext;
import org.openqa.selenium.WebDriver;

public class HomePageMenu extends AbstractMenu {

    public HomePageMenu(WebDriver driver, SearchContext searchContext) {
        super(driver, searchContext);
    }
}
